1.保存状态，避免重新计算。
2.将信息预处理到数据结构中。
3.分治算法。
4.扫描算法。有关数组的问题经常可以通过询问“我如何将x[0...i-1]的解决方案扩展为x[0...i]的解决方案。”的方式得到解决。算法3同时保存旧的答案和一些辅助数据，以计算新答案。
5.累积。使用累计表，在累计表中，第i个元素包含x中前i个值的总和。
6.下限。只有了解到他们的算法已经是可能算法中的最佳时，算法设计师才可能安稳的睡个好觉。为此，他们必须证明某个匹配下限。